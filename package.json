{
  "name": "appsync-resolverscript",
  "version": "1.0.0",
  "description": "Write AWS appsync resolvers in JavaScript.",
  "author": "alex <me@alexmcmanus.co.uk>",
  "repository": "https://github.com/alexmcmanus/appsync-resolverscript",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "modern test",
    "format": "modern format",
    "lint": "modern lint",
    "precommit": "modern precommit",
    "build": "tsc",
    "ci": "CI=true yarn build && CI=true yarn test",
    "prepublish": "yarn ci"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.20",
    "husky": "^3.0.9",
    "modern-node": "^2.5.1",
    "ts-jest": "^24.1.0",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn precommit",
      "pre-push": "yarn ci"
    }
  }
}
